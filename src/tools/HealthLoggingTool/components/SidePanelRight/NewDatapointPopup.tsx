// Import the Standard Units from the Units script
import Units from '../../scripts/Units';

function NewDatapointPopup() {
    return (
        <div className="new-datapoint-popup">
            <p className="panel-title">Add a new data point</p>
            {/* Form for adding a new data point */}
        </div>
    );
}

export default NewDatapointPopup;